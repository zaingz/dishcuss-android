<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.holygon.dishcuss">


    <!-- Maps API needs OpenGL ES 2.0. -->
    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />

    <permission
        android:name="com.holygon.dishcuss.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.holygon.dishcuss.permission.MAPS_RECEIVE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Pic Selection and Upload -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:name=".Utils.DishCussApplication"
        android:supportsRtl="true"
        android:theme="@style/MyMaterialTheme">


        <activity android:name=".Activities.SplashActivity"
            android:theme="@style/MySplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".Activities.FindEatBuddiesLoginFirstActivity"
            android:theme="@style/MyMaterialTheme"/>

        <activity
            android:name=".Activities.FriendsList"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.LoginActivity"/>
        <activity
            android:name=".Activities.SignInActivity"
            android:theme="@style/MyMaterialTheme"/>
        <activity
            android:name=".Activities.SignupActivity"
            android:theme="@style/MyMaterialTheme"/>

        <activity
            android:name=".Activities.SelectRestaurantActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.PostDetailActivity"
            android:windowSoftInputMode="adjustResize|stateVisible"
            android:theme="@style/MyMaterialTheme"/>


        <activity
            android:name=".Activities.BookmarkActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.ExploreSelectedCategoryActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.SelectRestaurantSearchActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".ExtraClasses.SearchMainActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.SearchUserAndRestaurantActivity"
            android:theme="@style/MyMaterialTheme" />
        <activity
            android:name=".Chat.ChatScreenActivity"
            android:theme="@style/MyMaterialTheme"
            android:windowSoftInputMode="adjustPan"/>


        <activity
            android:name=".Activities.NotificationActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.HomeActivity"
            android:theme="@style/MyMaterialTheme"/>

        <activity
            android:name=".Activities.RestaurantDetailActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.UpdateProfileActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.GetFreeFoodActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.ScanQRCodeActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.ProfilesDetailActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.UserOffersActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.FindYourEatBuddiesActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.KhabaHistoryActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.MyWalletActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Activities.StatusActivity"
            android:theme="@style/MyMaterialTheme"/>

        <activity
            android:name=".Posts.WriteReviewPostActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Posts.CheckInPostActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Posts.CheckInPostGoogleApiActivity"
            android:theme="@style/MyMaterialTheme" />

        <activity
            android:name=".Posts.PhotoPostActivity"
            android:theme="@style/MyMaterialTheme" />

        <!--<activity-->
            <!--android:name=".Activities.StatusActivity"-->
            <!--android:theme="@style/Theme.Transparent"/>-->

        <activity
            android:name=".Posts.PostSelectionActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"/>

        <activity
            android:name=".Activities.PhotoDetailActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen"/>

        <activity
            android:name=".Activities.PunditSelectionActivity"
            android:theme="@style/MyMaterialTheme"/>


        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:label="@string/app_name" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_map_id"/>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="80eafe8e17874d3e025e50743e1de478e8ecb5df" />

    </application>

</manifest>

<!--<activity android:name=".Activities.HomeActivityss"-->
<!--android:label="Add new friend"-->
<!--android:theme="@style/MyMaterialTheme" >-->